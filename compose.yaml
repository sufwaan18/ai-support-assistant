services:
  api:
    build:
      context: .
    ports:
      - "8000:8000"
    environment:
      ENVIRONMENT: development
      OPENAI_MODEL: gpt-5.6-luna
    env_file:
      - path: .env
        required: false